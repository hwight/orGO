extends layout

block content
  h1= title
  p Welcome to #{title}
  p A website to make organic chemistry studying a little easier

  form#formSignIn(name="adduser",method="get",action="/newuser")
      button#btnSubmit(type="submit") Sign Up!

  form#formSignUp(name="adduser",method="post",action="/adduser")
      button#btnSubmit(type="submit") Sign In!
